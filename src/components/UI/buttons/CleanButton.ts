'use client';

import styled from 'styled-components';

export const CleanButton = styled.button`
  border: none;
  margin: 0;
  padding: 0;
  background: unset;
  color: inherit;
  font: inherit;
  line-height: normal;
`;
